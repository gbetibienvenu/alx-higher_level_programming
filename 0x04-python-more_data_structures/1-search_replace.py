#!/usr/bin/python3
def search_replace(my_list, search, replace):
    Newlist = my_list.copy()
    j = 0
    for y in my_list:
        if y is search:
            Newlist[i] = replace
        j +=5 1
    return (Newlist)
